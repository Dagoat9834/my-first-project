<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Chill Zone</title>
<style>
    body {
        margin: 0;
        font-family: "Segoe UI", sans-serif;
        background: linear-gradient(135deg, #c9e4ff, #f7e9ff, #d9fff3);
        background-size: 300% 300%;
        animation: drift 18s ease infinite;
        height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        color: #2a2a2a;
        text-align: center;
    }

    @keyframes drift {
        0% { background-position: 0% 50% }
        50% { background-position: 100% 50% }
        100% { background-position: 0% 50% }
    }

    h1 {
        font-size: 3rem;
        margin-bottom: 10px;
        font-weight: 600;
    }

    p {
        font-size: 1.2rem;
        opacity: 0.8;
        margin-bottom: 30px;
    }

    button {
        padding: 12px 28px;
        font-size: 1.1rem;
        border: none;
        border-radius: 30px;
        background: rgba(255, 255, 255, 0.7);
        backdrop-filter: blur(6px);
        cursor: pointer;
        transition: 0.3s;
    }

    button:hover {
        background: rgba(255, 255, 255, 0.9);
        transform: translateY(-3px);
    }

    #quote {
        margin-top: 25px;
        font-size: 1.4rem;
        opacity: 0;
        transition: opacity 0.6s;
        max-width: 500px;
        line-height: 1.5;
    }
</style>
</head>
<body>

<h1>Welcome to the Chill Zone 🌿</h1>
<p>Take a breath. Slow down. Stay a while.</p>

<button onclick="showQuote()">Give me a chill thought</button>

<div id="quote"></div>

<script>
    const quotes = [
        "Breathe in calm, breathe out noise.",
        "You don’t have to rush. The moment is enough.",
        "Quiet minds see the clearest.",
        "Soft moments matter more than loud ones.",
        "Let today be gentle."
    ];

    function showQuote() {
        const q = document.getElementById("quote");
        q.textContent = quotes[Math.floor(Math.random() * quotes.length)];
        q.style.opacity = 1;
    }
</script>

</body>
</html>
